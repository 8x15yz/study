// 1. querySelector         (단일element 반환)
// 2. querySelectorAll      (NodeList 반환)
// 둘다 element를 선택하는 메서드 => id + class + tag 선택자 모두 인풋값으로 사용 가능함

// 3. getElementById        (단일element 반환)
// 4. getElementsByName     (HTMLCollection 반환)
// 5. getElementByClassName (HTMLCollection 반환)

// HTMLCollection : name, id, index 속성으로 각 항목에 접근 가능 
// NodeList : index로만 각 항목에 접근 가능 + 배열 관련 메서드 사용 가능

